[package]
name = "iagent-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
iagent-message-types = { path = "../iagent-message-types" }
serde = { version = "1", features = ["derive"] }
